Usages: list [options] [arguments]

Options:
  -h|--help     Get help.
  -o|--owner    Repository owner(default from pull url).
  -r|--remote   Use specify remote(default origin).
  -s|--state    Use specify state of pull requests:
                  open(default)
                  closed
                  all.